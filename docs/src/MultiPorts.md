```@docs
add_C_multiport!(BG::BondGraph, elements, parameters, name; ϕi = (e, q, params, BG) -> [], ϕk = (e, q, params, BG) -> [])
add_I_multiport!(BG::BondGraph, elements, parameters, name; ϕi = (p, f, params) -> [], ϕk = (p, f, params) -> [])
```